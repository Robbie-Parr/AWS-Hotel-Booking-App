import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    View,
  } from 'react-native';

const SplashScreen = () => {
    return(
        <View>
            <Text>Splash Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({
  
});

export default SplashScreen;