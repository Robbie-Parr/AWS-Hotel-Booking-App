import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    View,
  } from 'react-native';

const BookingScreen = () => {
    return(
        <View>
            <Text>Booking Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({
  
});

export default BookingScreen;