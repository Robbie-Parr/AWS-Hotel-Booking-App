import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    View,
  } from 'react-native';

const AdminScreen = () => {
    return(
        <View>
            <Text>Admin Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({
  
});

export default AdminScreen;