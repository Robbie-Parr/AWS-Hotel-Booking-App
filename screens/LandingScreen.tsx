import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    View,
  } from 'react-native';

const LandingScreen = () => {
    return(
        <View>
            <Text>Landing Screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({
  
});

export default LandingScreen;